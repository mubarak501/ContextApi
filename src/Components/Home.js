import React from "react";
import Button from "@material-ui/core/Button";
const Home = (props) => {
  return (
    <div>
      <Button onclick={props.addHome}>Homasdasfae</Button>
    </div>
  );
};
export default Home;
